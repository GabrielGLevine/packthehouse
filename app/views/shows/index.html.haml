%h1 Listing shows

%table
  %thead
    %tr
      %th Name
      %th Time
      %th Location
      %th Tickets
      %th Price
      %th
      %th
      %th

  %tbody
    - @shows.each do |show|
      %tr
        %td= show.name
        %td= show.time.to_formatted_s(:long_ordinal)
        %td= show.location
        %td= show.tickets
        %td= number_to_currency(show.price, unit: '$')
        %td= link_to 'Show', show
        %td= link_to 'Edit', edit_show_path(show)
        %td= link_to 'Destroy', show, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Show', new_show_path
